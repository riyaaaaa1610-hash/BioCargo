import {createFileRoute,useNavigate} from "@tanstack/react-router";import {useState} from "react";import {Button} from "@/ui/button";import {Input} from "@/ui/input";import {Label} from "@/ui/label";export const Route=createFileRoute("/signup")({component:Signup});function Signup(){const n=useNavigate();const[u,su]=useState("");const[p,sp]=useState("");const[r,sr]=useState("Airport Operator");return <div className="min-h-screen flex items-center justify-center"><div className="p-8 rounded-xl bg-panel ring-1 ring-line w-96"><h1 className="text-2xl font-bold mb-4">Create Account</h1><Label>Username</Label><Input value={u} onChange={e=>su(e.target.value)}/><Label>Password</Label><Input type="password" value={p} onChange={e=>sp(e.target.value)}/><Label>Role</Label><select className="w-full border rounded p-2 mt-1" value={r} onChange={e=>sr(e.target.value)}><option>Admin</option><option>Airport Operator</option><option>Medical Logistics Officer</option></select><Button className="w-full mt-4" onClick={()=>{sessionStorage.setItem("biocargo_user",u);sessionStorage.setItem("biocargo_role",r);n({to:"/"});}}>Sign Up</Button></div></div>}